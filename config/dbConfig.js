// import { dbUri } from "./envConfig";

// import mongoose from "mongoose";
// const connection = mongoose.connect(dbUri).then(() => {
//   console.log("mongodb connected successfully");
// }).catch((error)=>{console.log(error)});
// export default connection;